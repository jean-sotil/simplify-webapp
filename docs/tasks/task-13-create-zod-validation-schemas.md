---
id: TASK-13
phase: "Phase 1 — Next.js Scaffold (Day 1)"
title: "Create Zod validation schemas"
status: pending
---

# TASK-13 — Create Zod validation schemas

## Phase
Phase 1 — Next.js Scaffold (Day 1)

## Description
Write `src/lib/validation/schemas.ts` with `CreateProjectSchema`, `UpdateProjectSchema`, `DocumentUploadSchema` (max 50 MB, `.pdf` only), and `SelectedDocumentSchema`. Infer and export TypeScript types from all schemas.

## Acceptance Criteria
`z.parse()` on valid and invalid fixtures behaves correctly.
