---
id: TASK-33
phase: "Phase 4 — Documents Module (Days 5–6)"
title: "Build DocumentUploader component"
status: pending
---

# TASK-33 — Build DocumentUploader component

## Phase
Phase 4 — Documents Module (Days 5–6)

## Description
Create `src/components/documents/DocumentUploader.tsx` (`use client`). File input accepts `.pdf` only. Dropdown selects `ett` or `hardware`. Shows upload progress state, success confirmation, and error message. Disables inputs during submission. All form controls are labeled.

## Acceptance Criteria
Drag-and-drop and click-to-select both trigger the file input; error shown if non-PDF selected.
